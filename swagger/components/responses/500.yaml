description: Internal server server error
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 500
        message:
          type: string
          example: Something went wront
        data:
          type: string
          example: Error details or message from the server   
